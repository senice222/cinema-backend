{
  "name": "@cinema/contracts",
  "version": "1.0.0",
  "description": "Protobuf definitions for the cinema project",
  "main": "index.js",
  "scripts": {
    "generate": "protoc -I ./proto ./proto/*.proto --ts_proto_out=./gen --ts_proto_opt=nestJs=true,package=omit"
  },
  "files": [
    "proto",
    "gen"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@nestjs/microservices": "^11.1.12",
    "rxjs": "^7.8.2"
  }
}
